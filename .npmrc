# pnpm configuration
# https://pnpm.io/npmrc

# Automatically install peer dependencies
auto-install-peers=true

# Don't fail on peer dependency version mismatches (allows more flexibility)
strict-peer-dependencies=false

# Use pnpm's default symlink mode for better binary handling
# node-linker=isolated (default - commented out)

# Hoist specific packages for IDE integration and build compatibility
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=*vite*
public-hoist-pattern[]=*rollup*
public-hoist-pattern[]=*@remix-run*
public-hoist-pattern[]=*react-router*

# Store location (uses default ~/.pnpm-store)
# store-dir=~/.pnpm-store

# Use shamefully-hoist for maximum Vite/Rollup compatibility
# Creates a flat node_modules structure like npm
shamefully-hoist=true
